// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  webpack5: true,
  experimental: true,
  compress: true,
  poweredByHeader: false,
}

module.exports = nextConfig
